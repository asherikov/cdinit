type = process
load-options = sub-vars
command = sleep $CDINIT_TIMEOUT

# ROS waits 15 second
stop-timeout = 20.0

# let it output to console
options = shares-console always-chain
chain-to = shutdown
